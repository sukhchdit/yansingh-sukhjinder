import { AdverseEvent } from "../../esource/adverseevent.model";

export class SaveAdverseEventViewModel {
  constructor() {
    
  }
  adverseEvent: AdverseEvent;
  title: string;
  comments: string;
}
